[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]

[packages]
openpyxl = "*"

[requires]
python_version = "3.8"

[scripts]
convert = "python xlsx2json/xlsx2json.py"
vet = "cue vet vet/data.cue out/data.json"
all = "pipenv run convert && pipenv run vet"